// Middleware removed - was causing redirect loops
// This was demo protection code that redirected non-hackathon users
// Removed to allow normal development access

export function middleware() {
  // No middleware needed for development
}
